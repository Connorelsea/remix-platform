subscription newFriendRequest($toUserId: ID!) {
  newFriendRequest(toUserId: $toUserId) {
    id
    fromUser {
      id
      name
      username
      color
    }
    toUser {
      id
    }
    message
    createdAt
  }
}
